import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  container: {
    height: '100%',
    padding: 20,
  },
  head: {
    fontSize: 24,
    fontWeight: '600',
    marginBottom: 20,
  },
});
